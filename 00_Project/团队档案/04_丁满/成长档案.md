# 🐾 丁满 Timon｜成长档案

## 2026-08-20｜固定高程与自动限高

- 将 TrailPrint3D `fixedElevationScale` 接入 Web，新增独立高度策略与单元测试。
- 同时记录请求倍率和有效倍率，使不同 GPX 的厚度可预测、可回归。
- 修复 Blender `--python` 模式下工具模块搜索路径缺失的问题。

## 2026-08-11｜通用 GPX 生产流水线

- 成果：新增路线无关、可断点复用的 15 阶段流水线，实现新 GPX 到 5×3MF + 1×Blend。
- 失败与修正：发现 Blender 重导 STL 会删除三角面并改变拓扑统计；取消自动改写，改为只读审计，将发布判断移到最终 Bambu 3MF 的平台/Z/ZIP 检查。
- 验证：大夫山第二作品独立生成并通过 `generic_release_qa.json`，证明不再依赖星溪专用模型。
- 能力成长：从“封装固定样板”提升为“按输入建任务、逐阶段恢复、自动 QA 与 Web 下载”。

## 2026-08-12｜水系装配公差固化

- 真机事实：水系槽 `0.12 mm/侧` 安装困难且容易折断；`0.25 mm/侧` 安装非常顺利；
- 成果：将水系公差与轨迹公差拆分为独立参数，并在加宽/加固后几何上同源生成槽体；
- 状态：`0.25 mm/侧` 已是星溪线真机验证默认值，待其他 GPX 复验后升格为通用基线。

角色：自动化、参数化和流程复用负责人  
默认模型：🌍 Terra / 🌙 Luna  
升级模型：☀️ Sol，用于新流水线架构和复杂失败恢复

## 能力基线

| 能力 | 等级 | 证据 |
|---|---:|---|
| 专项脚本化 | 5 | `07_Knowledge/tools/` 的完整工具链 |
| 批量处理 | 4 | OSM候选、GeoJSON、道路、水体和3MF批处理 |
| 自动验证 | 4 | JSON、几何、ZIP、哈希和坐标检查 |
| 参数化 | 3 | 部分工具有参数，仍含 S02 专用常量 |
| 失败恢复 | 3 | 已使用落盘和checkpoint，尚无统一状态机 |
| 一键产品流水线 | 2 | 尚未实现 GPX → 4×3MF |

## S02 成果

- 建立 Overpass probe、GPX读取和水体距离分析工具；
- 建立模型边界提取、裁剪和打印筛选工具；
- 建立水体、道路、村庄、轨迹、底座和文字 Blender 脚本；
- 建立多色零件导出和 3MF 修复工具；
- 建立文字居中和俯视 QA 工具；
- 建立低 Token 复用流程；
- 将成功原始数据落盘，避免重复请求和会话依赖。

成果证据：

- `07_Knowledge/tools/`
- `07_Knowledge/Conclusions/徒步地形模型_低Token复用流程.md`
- `07_Knowledge/Conclusions/水体获取失败与成功复盘.md`

## 关键成长

### 从临时命令转向可复现脚本

水体、坐标、文字和 3MF 问题不再只留在一次操作中，而是转成可再次运行的工具。

### 从“成功就继续”转向成功即落盘

Overpass 成功后保存原始 JSON、GeoJSON 和报告，使后续工作不依赖公共 API 的
实时状态。

### 从重复探索转向低 Token 路由

总结固定参数、最短执行链、优先工具和 checkpoint，使下一条路线不必重新扫描
全部 S02 历史。

## 当前短板

- 多数脚本由固定路径和 S02 对象名连接；
- 缺少统一 `job.json` 数据契约；
- 缺少任务状态机、幂等、重试和阶段恢复；
- 缺少统一 CLI 和结构化错误码；
- 缺少自动化测试夹具和跨路线回归集；
- 尚未形成可安装的应用或 Worker。

## 下一阶段目标

1. 定义 `job.schema.json` 和样例 `job.json`；
2. 把 S02 常量提取成参数；
3. 建立 `prepare → gis → review → blender → package → qa` 状态机；
4. 每阶段支持幂等重跑和 checkpoint；
5. 实现一个命令输出四个 3MF；
6. 用第二条 GPX 验证无需修改代码。

## 成长记录

### 2026-07-25｜S02｜建立可恢复的水体获取

- 使用模型：🌍 Terra；重复验证适合 🌙 Luna；
- 失败：第三方依赖缺失、查询语义过窄；
- 修正：标准库、多节点、正向对照、原始落盘；
- 能力变化：失败恢复 1 → 3；
- 证据：`07_Knowledge/tools/probe_osm_water.py`。

### 2026-07-28｜V010｜固化文字居中算法

- 使用模型：🌍 Terra；
- 新掌握：从基座几何计算文字目标，不使用手填坐标；
- 能力变化：专项脚本化 4 → 5；
- 证据：`07_Knowledge/tools/update_three_edge_labels.py`。

### 2026-07-28｜SYS01｜评估自动化成熟度

- 使用模型：🌍 Terra，批量盘点适合 🌙 Luna；
- 已确认：40个工具中28个已有参数入口，但29个仍含案例专用标识；
- 当前等级：已知案例可重复生成，有人工串联，自动化3/5；
- 新目标：统一 `job.json`、状态机、manifest和纯净3MF封装；
- 证据：`00_Project/SYS01_分件分色自动化评估.md`。

### 2026-07-28｜SYS01_V001｜建立3MF角色净化工具

- 使用模型：🌍 Terra；
- 新掌握：同步修改根模型、build、Bambu配置、plate实例、assemble和关系文件；
- 产出：可复用的 `prune_bambu_3mf_objects.py`；
- 结果：在不重建几何的情况下删除沙盘3MF中的重复轨迹；
- 能力变化：参数化 3 → 4；
- 证据：`06_Experiment/SYS01_TerrainPackagingPrototype/process/SYS01_V001_Prune_Report.json`。

### 2026-07-28｜SYS01_V002｜自动生成水体件与安装槽

- 使用模型：🌍 Terra；
- 产出：`build_water_inserts_and_grooves.py` 与预览渲染工具；
- 失败：EXACT 布尔结果产生非流形边，被 QA 拒绝；
- 修正：改用 MANIFOLD 求解并复检，三段地形和水体非流形边均为 0；
- 成长：把布尔求解器选择和失败退出纳入可制造流程；
- 证据：`07_Knowledge/tools/build_water_inserts_and_grooves.py`。

### 2026-08-10｜大夫山 V002｜尺寸适配参数化

- 新增/增强：3MF 网格测量、提取、渲染工具，以及 STL/Blender 统一 XY 缩放；
- `layout_job_print_parts.py` 新增 `--xy-scale`，明确保持 Z 不变；
- 成长：将一次性的第三方框体适配沉淀为可复用尺寸管线；
- 证据：`07_Knowledge/tools/inspect_3mf_mesh_dimensions.py`、`07_Knowledge/tools/scale_blender_scene_xy.py`。

### 2026-08-10｜星溪线｜干净 job 目录全链回归

- 从同一 GPX 的新 job 目录重新生成工程输出，没有复制旧 final；
- 暴露并修正“场景无旧底座”和“世界坐标/局部 STL 重复归零”两个隐含状态；
- 工程参数化通过，但诚实保留“尚无单命令编排器”的产品缺口；
- 证据：`08_Jobs/20260810_xingxi_parameter_regression/review/参数化一次性回归报告.md`。

### 2026-07-29｜SYS01_V005｜补齐设计图到 Blender 的矢量桥梁

- 失败认识：PNG 可作参考或贴图，但不能直接成为 FDM 多色打印区域；
- 新工具：`raster_logo_to_svg.py`，把高对比设计图追踪为闭合 SVG；
- 新链路：设计图 → SVG → Blender 实体 → 开槽 → 3MF；
- 结果：15 个设计路径直接进入底座，停止用基本体重新拼画；
- 证据：`06_Experiment/SYS01_TerrainPackagingPrototype/review/SYS01_V005_SVG_Logo_Review.md`。

## 2026-08-10｜水系增强 R8 参数化

- 将地形 resolution、水体类别和水体距离阈值从脚本硬编码迁移到 `job.json`。
- 新增 `repair_stl_mesh.py`，可保守修复 STL 合并后出现的孤立非流形边；本次将蓝色水件从 1 条修复为 0 条。
- 收益：以后调整水系密度与地形精度无需再次修改生成逻辑。

## 2026-08-11｜TrailPrint 真机参数基线

- 将用户已打印通过的两张截图参数固化为生成器默认值与项目唯一基线。
- 移除三色地形阶段额外的 1.50× Z 放大，避免破坏 Elevation Scale 1.80。
- 修复 1.60 mm 轨迹与端点 relief 做 Exact UNION 时丢失长轨迹组件的问题。

## 2026-08-11｜WEB01 本地参数工作台

- 将真机基线、TrailPrint3D 数据约束和 Bambu 5+1 交付清单收敛到本地 Web 页面；
- 新增 GPX 浏览器端解析与 `job.json` 导出，为单命令编排器建立统一输入；
- 在前端依赖安装受阻后，改为零依赖静态运行版本，保证本地可启动和可移交；
- 诚实保留“Blender 与 Bambu 尚未由页面直接调用”的边界，下一步转向本地任务服务；
- 证据：`09_WebApp/`、`00_Project/WEB01_本地工作台阶段记录.md`。

### WEB01.1 修正｜本地部署等于 Web 联动 Blender

- 修正产品定义：本地 Web 不是配置导出页，而是 Blender 本地仿真与生成控制台；
- 接通页面 → 本地任务服务 → TrailPrint3D → Blender → 真实 PNG/Blend 回传；
- 快速预览开始使用上传 GPX 的真实平面形状，并与 Blender 真实渲染严格区分；
- 增加四阶段“上一步 / 下一步”和用户可执行验收清单。
- 真机浏览器验收发现中文任务名在图片 URL 中被百分号编码，服务端按原始路径匹配导致 404；修正为先解析 URL、再解码与白名单校验，预览接口恢复 200。
- 用户验收推动仿真从单张“好看图”升级为装配、顶视、侧视三机位，并增加交付覆盖状态；页面开始明确区分真实几何、参考体和待生成成果。

### 2026-08-11｜WEB01.5｜从仿真页走到可下载交付

- 新增最终任务接口、逐件下载、完整 ZIP、最近成果恢复与中文附件名支持；
- 对 5 个 3MF 做 ZIP 完整性检查，对 Blender 5 的 Zstandard 压缩格式建立文件头识别；
- 将 GPX 哈希和八项真机参数作为黄金样板复用门槛，非匹配任务使用结构化错误阻止误交付；
- 成长：一键产品流水线 2 → 3（星溪黄金样板已闭环；通用 GPX 仍需单命令编排器）；
- 证据：`09_WebApp/local/server.py`、`08_Jobs/WEB_20260811_161150_星溪竹林/review/final_manifest.json`。
## 2026-08-13｜四对象自动门禁

- 新增 `validate_bambu_four_object_plate.py`，将四顶层对象、七材料子件固化为可重复 PASS/FAIL 检查；
- 已接入 Web 通用任务流水线和发布验证，避免后续 GPX 再退化为七件。
## 2026-08-13｜TrailPrint3D 退化轨迹自动恢复

- 风车山 GPX 暴露出 TrailPrint3D 返回 `1 顶点 / 0 面` 假成功的边界情况；
- 新增有效面检测，并复用生成前保留的顺 GPX 承力芯恢复轨迹实体；
- 保持跨地形直线桥数量为 0，风车山通用流水线 `PIPELINE_PASS`。
## 2026-08-13｜三路线一键全面回归

- 将星溪、大夫山、风车山固化为按 GPX SHA-256 匹配的回归样本；
- 新增多路线统一入口，自动汇总 5+1、地形、轨迹、水系、四对象、颜色、Blender 和三机位证据；
- 首轮发现大夫山水系 13 组件，证明自动回归能识别人工只看四件同盘时遗漏的系统问题。
- 根据当前阶段“可用优先”目标，将回归结果固化为 `BLOCKER / WARNING / ADVISORY` 三级；只有阻断项返回失败，水系超过 5 件保留为警告。
- 第二轮结果为 `PASS_WITH_WARNINGS`：星溪、风车山 PASS，大夫山仅水系 13 件警告。

## 2026-08-15｜长距离参数路由与兼容回归

- 新增 `configure_route_scene.py`，只对非局部路线生成派生 GPX 和自适应参数；
- 新增 `test_adaptive_route_profiles.py`，自动证明三个真机/人工通过基线参数不变；
- 14859 点区域轨迹降为 64 个建模控制点，源数据保持不变；
- 证据：`00_Project/回归测试/自适应路线分级_20260814.json`。

## 2026-08-20｜奖牌框适配方法固化到通用流水线

- 将通用底座阶段从“地形外扩”生成器切换为奖牌框实测轮廓生成器；
- 保持 GPX、地形、轨迹和水系阶段不变，使后续 Web 新任务自动继承正确底座方法；
- 后台 Blender 5.2 偶发 Metal 初始化崩溃被识别为需要增加有限重试的工具链稳定性事项。

## 2026-08-20｜WEB05 异步任务与前后端分离

- 将 Blender 预览和最终 5+1 从同步 HTTP 请求改为持久化单 Worker 队列；
- 固化 `QUEUED / RUNNING / PREVIEW_READY / FINAL_READY / FAILED` 状态机、原子状态文件、重启恢复和重复提交保护；
- 拆分 4173 静态前端与 4174 API，增加 CORS、运行时 API 配置和 PID/日志运维脚本；
- 轻量冒烟证实提交约 0.1 秒返回，后台失败原因独立落盘；
- 证据：`09_WebApp/local/async_jobs.py`、`09_WebApp/manage_split.py`、`00_Project/WEB05_前后端分离部署方案_2026-08-20.md`。

## 2026-08-20｜WEB06 匿名多任务单页工作台

- 将四步跳转改为所有阶段常驻的单页工作台，并为各阶段预留实时摘要；
- 新增匿名设备 UUID、多任务列表、跨刷新恢复、任务切换和继续上传；
- 幂等键加入客户边界，避免不同匿名客户错误复用同一任务；
- 参考成熟队列/任务模型并加入可访问性实时状态区域；
- 证据：`09_WebApp/local/single-page.css`、`00_Project/WEB06_匿名多任务单页工作台_2026-08-20.md`。

## 2026-08-20｜WEB07 双阶段图墙与队列阶段统计

- 将单一三机位画廊拆成工程仿真和最终交付两套持久图位，最终恢复不再覆盖前一阶段；
- 队列快照新增 `stage_counts`，前端原位显示运行数、等待数和七个流水线阶段；
- 用显式 CSS Grid 行消除旧版 `order` 规则串扰，1440×900 实测无页面级滚动；
- 证据：`09_WebApp/local/dashboard.css`、`00_Project/WEB07_双阶段图墙与单屏队列_2026-08-20.md`。
- WEB07.1 移除 1600 px 宽度上限，增加宽屏 A/B 并排布局、六边形 16:9 预留位、阶段状态原位更新
  和点击放大对话框；1280×720 回归保持单屏完整。
- WEB07.2 根据用户宽屏验收推翻“A/B 并排即利用空间”的假设；将两阶段改为上下大图墙、
  左右栏改为内容驱动高度，并增加 720p 紧凑规则。2048×1120 下单图从约 418×74 px
  提升到 418×285 px，证明布局验收必须同时量化容器高度与有效图片面积。

## 2026-08-20｜WEB07.3 状态并发竞态修复

- 从“页面报失败但 Worker 继续运行”的矛盾证据定位到 `job.json` 非原子覆写窗口；
- 将路线配置阶段的任务元数据写入改为临时文件原子替换，并在前端为新任务增加有限 404 重试；
- 新增 1000 次并发读写回归，确保任务轮询不会再读到半截 JSON；
- 成长：异步任务不能只保证队列持久化，还必须把元数据写入原子性和客户端瞬态容错纳入工具链。

## 2026-08-21｜WEB08 上海同源代理与流式交付

- 将上海静态前端扩展为同源 API/预览/下载网关，浏览器无需直接暴露计算后端地址；
- 修正大文件“整包缓冲 + 30 秒超时”，改为 1 MiB 分块流式转发和参数化上游超时；
- 形成 release 目录、`current` 软链接、反向 SSH 的可回滚部署骨架；
- 成长：下载 QA 必须实际拉取全包并核对 Content-Length、SHA 和 ZIP，而不能只相信生成接口。
# 2026-08-25｜奖牌框规格单一来源

- 将奖牌框六顶点、公差、设计轮廓、打印网格和可见环宽集中为版本化规格模块。
- 发布 QA 同时阻止过大、误缩小、环宽漂移与规格版本漂移，减少后续路线重复调参。

## 2026-08-25｜WEB13 创意选择持久化工具

- 新增 `build_creative_choices.py`，从任务 GPX 生成可恢复的标题、Logo 与底面校样派生数据；
- 将 `TITLE_SELECTION → LOGO_SELECTION → CONFIRMATION → CONFIRMED` 固化为可测试状态机；
- 最终制造只有在确认状态满足时才运行，并复用客户选择的原始 SVG；
- 证据：`09_WebApp/local/test_creative_workflow.py`。

## 2026-08-26｜打印表面与水系策略参数化

- 将“禁止人工跨地形水系连接桥”固化为显式策略字段和流水线修订号，避免旧成果复用。
- 将顶面平滑固化为可审计参数：4 次、0.38 系数、单步 0.22 mm 上限，并输出前后网格统计。
- 重复修模由手工判断转为脚本可复现、QA 可拦截的生产规则。

## 2026-08-25｜中文任务路由与本地界面回归

- 从“标题已点击但仍显示未保存”定位到浏览器百分号编码与服务端路由解码不对称；
- 将创意请求绑定任务匿名客户 ID，避免刷新后前端身份变化造成状态错读；
- 用浏览器几何测量验证顶部路线图和三机位区不存在重叠，并把控制台零错误纳入本地验收；
- 成长：前后端状态机的 PASS 必须同时覆盖 URL 编码、身份边界、刷新恢复和真实布局，不能只验证接口返回。
