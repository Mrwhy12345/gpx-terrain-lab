# GPX Terrain Lab 协作规范

## 🦁 荣耀大地五人组

本项目采用五个职责角色。角色代表责任边界，LLM 代表当前任务难度；角色可以在
Sol、Terra、Luna 之间切换，不要求五个角色同时运行。

| 图标 | 角色 | 主要职责 | 默认模型 |
|---|---|---|---|
| 🦁 | 辛巴 Simba | 主线、任务拆分、版本、checkpoint、成果整合 | Terra |
| 🐒 | 拉菲奇 Rafiki | GPX、GIS、OSM、空间关系、几何与数据假设 | Terra |
| 🐗 | 彭彭 Pumbaa | Blender、网格、STL、3MF、Bambu 与打印公差 | Terra |
| 🐾 | 丁满 Timon | 自动化脚本、批处理、参数化和流程复用 | Terra / Luna |
| 🐦 | 沙祖 Zazu | QA、版本清单、哈希、文档、回归检查与封版 | Luna |

记忆口诀：

> 🦁定方向，🐒看土地，🐗造模型，🐾修工具，🐦守质量。

## 模型路由

- ☀️ **Sol**：新问题、疑难攻关、重大设计、连续失败、不可逆风险、复杂坐标或
  3MF 内部变换、封版前的关键技术判断。
- 🌍 **Terra**：默认主力，负责项目上下文、常规实现、GIS/Blender 工作和综合
  判断。
- 🌙 **Luna**：输入输出明确且容易验证的机械任务，例如盘点、批量检查、格式
  验证、统计、哈希和简单文档同步。

自动切换规则：

1. 同一问题连续失败两次，切换到 Sol；
2. 涉及封版文件、复杂坐标、3MF 装配或不可逆操作，优先 Sol；
3. 可由脚本给出明确 PASS/FAIL 的批量工作，优先 Luna；
4. 需要维护阶段上下文或跨专业整合，使用 Terra；
5. Sol 解决新问题后，由丁满将方法固化，使下次回到 Terra 或 Luna。

## 角色边界

### 🦁 辛巴

- 新阶段开始时先读当前 checkpoint；
- 明确目标、当前版本、不可变输入和下一步；
- 只有辛巴可以宣布当前发布版本或阶段完成；
- 阶段结束必须更新 checkpoint，不能让思路只留在聊天中。

### 🐒 拉菲奇

- 输出数据来源、坐标系、方法、事实、假设和筛选结论；
- 空间计算结果应提供 QGIS/Blender 可用的派生数据；
- 不直接决定打印结构、颜色或 3MF 版本；
- 不覆盖 GPX、OSM probe 等原始输入。

### 🐗 彭彭

- 输出 Blender 源文件、打印零件、几何报告和视觉核验图；
- 必查尺寸、Z 范围、非流形边、最薄特征和装配关系；
- 不覆盖封版文件；变化必须输出新版本；
- 3MF 内部变换应同时检查根模型和 Bambu 元数据。

### 🐾 丁满

- 重复出现的操作必须沉淀成脚本、参数或检查表；
- 优先复用 `07_Knowledge/tools/`，避免重新手工探索；
- 工具不得静默覆盖原始数据或封版成果；
- 新工具应留下最小输入、输出和验证方式。

### 🐦 沙祖

- 默认只读检查，输出 `PASS / FAIL / NEEDS_REVIEW`；
- 检查文件完整性、版本号、耗材、负 Z、ZIP、哈希和文档指向；
- 可以阻止封版，但不能擅自改变设计；
- 封版时生成 SHA-256 清单和 review 总结。

## 文件责任

| 路径 | 主责任角色 |
|---|---|
| `process/`、阶段 checkpoint | 🦁 辛巴 |
| GPX/OSM 分析及 `data/*_audit/` | 🐒 拉菲奇 |
| `result/`、`screenshots/` | 🐗 彭彭 |
| `07_Knowledge/tools/`、复用流程 | 🐾 丁满 |
| `review/`、校验和、封版报告 | 🐦 沙祖 |

同一时间尽量只有一个角色修改同一个文件。跨角色交付使用新文件或明确的阶段报告，
不依赖聊天中的隐含结论。

## 标准阶段顺序

1. 🦁 读取 checkpoint 并定义阶段目标；
2. 🐒 完成数据和空间事实；
3. 🐾 将可重复流程脚本化；
4. 🐗 生成实体和打印文件；
5. 🐦 独立检查；
6. 🦁 根据 QA 结果升版或退回；
7. 🐦 生成校验和，🦁 更新 checkpoint。

## 版本与封版

- 原始数据永不覆盖；
- 任一打印零件变化，完整套装统一升版；
- 已标记 `FINAL / SEALED` 的版本不可修改；
- 后续工作从封版版本复制并使用更高版本号；
- 阶段总结必须记录输入、事实、假设、决定、输出、未决项和下一步。

详细复用流程：

`07_Knowledge/Conclusions/徒步地形模型_低Token复用流程.md`

## 成长档案

五个角色的项目成果和能力成长统一记录在：

`00_Project/团队档案/README.md`

以下事件发生后必须更新对应成员档案：

- 有价值的失败与修正；
- 新增可复用工具；
- 跨角色正式交付；
- 真机验收；
- 版本封版；
- 模型升级或降级产生了明确收益。
