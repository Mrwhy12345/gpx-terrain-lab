# GPX Terrain Lab｜产品北极星目标

更新日期：2026-07-29

## 一句话目标

客户只上传一个 GPX 文件，系统自动生成一套可打印的徒步沙盘交付物；客户即使更换为 GLM、DeepSeek 等第一梯队模型，也能获得接近星溪线样板的工程质量与创意效果。

## 客户输入

### 必需

- 1 个有效 GPX 轨迹文件

### 可选

- 徒步名称或活动主题
- 日期、距离、队伍或纪念文字
- 偏好的视觉主题、Logo 元素与禁用元素
- 打印机、喷嘴、材料和可用颜色
- 高德地图核对信息或人工补充的地点信息

客户未填写可选信息时，系统应从 GPX 与地图上下文生成合理默认值，不阻塞流程。

## 固定交付

每个项目输出：

1. 沙盘地形 3MF
2. 底座 3MF
3. 徒步轨迹 3MF
4. 河流水系 3MF
5. 一盘逐件打印 3MF
6. 1 个完整 Blender 项目文件

`final/` 或正式发布目录只允许包含上述 6 个核心交付文件；报告、截图、中间 STL、缓存和诊断文件进入对应的 `process/`、`review/`、`work/` 或 `archive/`。

## 系统的两部分

### A. 工程系统

目标是稳定、可验证、尽量不依赖 LLM 的数值推理。

职责：

- 解析 GPX、轨迹方向、距离与范围
- 获取和裁切地形数据
- 生成具有合理垂直比例的沙盘地形
- 获取、筛选并核对水系
- 生成红色轨迹、蓝色水系及对应安装槽
- 处理轨迹与水系的相交、上跨和避让
- 生成等距底座、沙盘定位槽与磁铁孔
- 输出灰/棕底座及绿/棕/灰分层地形
- 封装 5 个 3MF 和 1 个 Blender 项目
- 执行确定性质量检查

最低自动验收：

- 坐标系和单位正确
- 最低 Z 为 0
- XY 包围盒在打印板内
- 非流形边为 0，或有明确的例外记录
- 地形由低到高为绿 → 棕 → 灰
- 水系为蓝、轨迹为红、底座为灰 + 棕
- 安装槽具有明确间隙参数
- 底座外框、凹槽和沙盘对应边平行
- 文字、Logo、轨迹和水系不侵入禁止区域
- Blender 与 3MF 使用同一套最终几何

### B. 创意系统

目标是根据路线语义生成有辨识度、可制造的项目表达。

输入上下文：

- GPX 文件中可提取的信息
- 高德地图核对结果
- 地形、水系、村落和路线特征
- 客户提供的可选文字与主题

职责：

- 生成徒步标题及备选标题
- 选择路线故事和视觉主题
- 设计底座底部 Logo
- 选择起点、终点符号
- 在颜色数量、喷嘴能力和最小特征尺寸内完成制造约束收敛

创意结果不能直接进入最终打印文件，必须先通过几何边界、最小笔画、连通性、安装避让和切片可见性检查。

## LLM 可替换原则

系统不得把工程正确性绑定到某一家模型。

### 不交给 LLM 决定

- 坐标转换
- 地形裁切
- 多边形偏移
- 安装间隙
- 包围盒
- Z 轴归零
- 网格拓扑
- 颜色与组件映射
- 文件封装和验收

这些能力必须由版本化代码、参数和自动测试实现。

### 允许 LLM 参与

- 地图信息归纳
- 水系候选的语义排序建议
- 标题文案
- Logo 概念与 SVG 草案
- 风格选择
- 异常解释和人工复核提示

所有模型通过统一的结构化输入和输出协议接入。模型输出不符合 JSON Schema、制造约束或自动检查时，系统必须拒绝进入最终构建。

## GitHub 下载版目标

客户应能够：

1. 下载 GitHub 项目。
2. 安装 Blender 和项目依赖。
3. 配置高德地图及地形数据源所需的密钥。
4. 选择 OpenAI、GLM、DeepSeek 或其他兼容模型。
5. 放入一个 GPX 文件。
6. 运行单一入口命令或图形界面。
7. 在固定输出目录获得 5 个 3MF 和 1 个 Blender 文件。

仓库必须提供：

- 一键环境检查
- `.env.example`
- 模型适配器接口
- 示例 GPX 与星溪线黄金样板
- 固定目录规范
- 参数配置文件
- 自动测试与发布验收
- 脱敏后的地图数据缓存或可重复获取说明
- 常见故障与恢复指南

## 成功标准

### 样板复现

同一 GPX、同一数据源和同一参数下，无论使用支持协议的哪种第一梯队 LLM：

- 工程几何结果必须一致或在规定公差内等价；
- 5 个 3MF 必须通过同一套打印检查；
- Blender 项目必须与打印件一致；
- 创意结果允许不同，但必须达到统一的审美与可制造评分阈值。

### 新路线泛化

更换 GPX 后，不修改路线专属代码即可完成构建。人工介入只用于：

- 地图数据歧义
- 水系或路线事实复核
- 客户创意选择
- 首件实物装配验收

## 当前样板的角色

星溪线 V012 是首个黄金样板候选，用于固化：

- 五文件打印交付结构
- Blender 完整预览
- 地形三层配色
- 五件以内精选水系
- 轨迹起终点符号
- 灰/棕双色底座
- SVG 竹林 Logo
- 等距六边形底座
- Z、XY、拓扑、颜色和装配避让检查

实物验收通过后，相关参数和检查才能从“样板经验”升级为“系统默认规则”。
